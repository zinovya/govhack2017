version: "2"
services:
    bump_map_backend:
        ports:
        - "80:4443"
        volumes:
          - ./db:/db
        build: backend
        entrypoint:
          - python
          - /main.py
